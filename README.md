
# 🛒 Hostel Trade

**Hostel Trade** — це full-stack веб-платформа (маркетплейс), розроблена в рамках лабораторних робіт з дисципліни **"Компоненти програмної інженерії"**.  
Платформа дозволяє студентам КПІ купувати та продавати речі в межах своїх гуртожитків.

---

## ✨ Основний функціонал

- **Автентифікація:** Повний цикл реєстрації, логіну та відновлення паролю через JWT (Access + Refresh Tokens).
- **Активація акаунту:** Підтвердження реєстрації через email (Nodemailer).
- **Керування оголошеннями:** C.R.U.D. (Створення, Редагування, Видалення) оголошень.
- **Завантаження зображень:** Завантаження фото для оголошень та аватарів профілю у сховище (Supabase Storage).
- **Пошук та Фільтрація:** Динамічний пошук з фільтрами за назвою, ціною, категорією та гуртожитком.
- **Профіль користувача:** Можливість переглядати та редагувати свій профіль.
- **"Обране":** Можливість "лайкати" оголошення та переглядати їх у своєму профілі.
- **Підрахунок переглядів:** Трекінг унікальних переглядів для кожного оголошення.

---

## 👥 Наша команда (Розподіл ролей)

Відповідно до вимог **Лабораторної роботи №4**, проєкт виконується командою з чітким розподілом ролей:

| Роль | Учасник | Обов’язки |
|------|----------|------------|
| 🧠 **Full-stack Lead** | `@RandomIv` | Архітектура проєкту, автентифікація, профіль, керування оголошеннями, код-рев’ю |
| ⚙️ **Backend Developer** | `@ErmishinS` | Розробка API (продукти, категорії, гуртожитки, "обране", перегляди), логіка фільтрації |
| 🎨 **Frontend Developer** | `@Ratarekko` | UI/UX, верстка сторінок (головна, пошук, картка товару), підключення до API |
| 🧪 **QA Engineer** | `@Ederatone` | Налаштування тестового середовища (Jest), написання unit тестів|

---

## 🛠️ Технологічний стек

### 🧩 Клієнт (Frontend)
- **React (Vite):** Сучасний збірник та середовище розробки.  
- **React Router (v6):** Маршрутизація на стороні клієнта з використанням `loader` та `action`.  
- **React Query (v5):** Управління станом сервера, кешування та синхронізація даних.  
- **CSS Modules:** Локальна ізоляція стилів компонентів.

### 🖥️ Сервер (Backend)
- **Node.js:** Асинхронне середовище виконання.  
- **Express.js:** Мінімалістичний веб-фреймворк для побудови API.  
- **Supabase:**  
  - PostgreSQL — реляційна база даних  
  - Supabase Storage — зберігання файлів (аватари, фото товарів)  
- **JWT (JSON Web Tokens):** Безпечна автентифікація (access + refresh токени).  
- **Nodemailer:** Надсилання листів (активація акаунту, скидання паролю).  
- **Pino:** Високопродуктивний логер.

---

## 📁 Архітектура проєкту

Проєкт побудований за принципом **Monorepo** (клієнт та сервер в одному репозиторії),  
з чітким розділенням відповідальності та дотриманням принципів **SOLID**.

### 🗄️ `server/`
Використовує **3-рівневу архітектуру**:
- `...Controller.js` — рівень API, приймає HTTP-запити.  
- `...Service.js` — рівень бізнес-логіки.  
- `...DAL.js` — рівень доступу до даних (працює з БД).

### 💻 `client/`
Використовує **сучасну компонентну архітектуру React**:
- `pages/` — сторінки, що відповідають роутам (з loader та action).  
- `components/` — перевикористовувані UI-компоненти.  
- `utils/` — допоміжні функції (HTTP-запити, валідація).  
- `hooks/` — кастомні React-хуки (наприклад, `useAuth`).

---

## 🚀 Як запустити проєкт

### 1. Клонування репозиторію
```bash
git clone https://github.com/RandomIv/hostel-trade.git
cd hostel-trade
````

---

### 2. Налаштування `.env`

Створіть файл `.env` у кореневій папці проєкту та додайте такий вміст:

```env
PORT=6000
DB_URL=
DB_KEY=
DB_NAME=
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
SENDER_EMAIL_USER=
SENDER_EMAIL_PASS=
CLIENT_URL=http://localhost:5173

```

---

### 3. Запуск сервера (Backend)

```bash
cd server
npm install
npm run dev
```

Сервер запуститься на [http://localhost:6000](http://localhost:6000).

---

### 4. Запуск клієнта (Frontend)

```bash
cd client
npm install
npm run dev
```

Клієнт запуститься на [http://localhost:5173](http://localhost:5173).
Відкрийте цей URL у вашому браузері.



